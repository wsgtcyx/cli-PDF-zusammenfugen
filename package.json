{
  "name": "pdfzus-merge",
  "version": "0.1.0",
  "description": "Command-line PDF merging tool inspired by the pdfzus browser utility.",
  "type": "module",
  "bin": {
    "pdfzus-merge": "dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist",
    "prepare": "npm run build"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "commander": "^12.0.0",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "license": "MIT"
}

